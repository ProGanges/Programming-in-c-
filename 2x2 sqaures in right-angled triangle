#include <iostream>
#include<cmath>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--)
	{
	    int b;
	    cin>>b;
	    b = b-2;
	    b=floor(b/2);
	    cout<<(b*(b+1)/2)<<endl;
	    
	}
	return 0;
}
